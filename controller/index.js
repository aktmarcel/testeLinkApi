const webhook = require('../controller/webhook');
const createDeal = require('../controller/createDeal');
const getDeal = require('../controller/getDeal');
module.exports ={
    webhook,
    createDeal,
    getDeal
}